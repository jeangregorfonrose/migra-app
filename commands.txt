# Command to generate translation files
flutter gen-l10n

# Run command, add access token key
flutter run --dart-define=ACCESS_TOKEN=

# Generate icons
flutter pub run flutter_launcher_icons

# specific device run
flutter run -d 